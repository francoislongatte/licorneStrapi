{
  "defaultConnection": "default",
  "connections": {
    "default": {
      "connector": "strapi-hook-mongoose",
      "settings": {
        "client": "mongo",
        "host": "${process.env.DATABASE_HOST || 'licorne-sa.documents.azure.com'}",
        "port": "${process.env.DATABASE_PORT || '10255'}",
        "database": "${process.env.DATABASE_NAME || 'strapi'}",
        "username": "${process.env.DATABASE_USERNAME || 'licorne-sa'}",
        "password": "${process.env.DATABASE_PASSWORD || 'cEgnyhVg1wjNGQIFqcJ86s1LFXEECcckYuTXLLezitFkUsvUNYcG1b1Fp5k0XAsPzc0VHR9Bm6eaRtZ2Q34Zfg=='}"
      },
      "options": {
        "authenticationDatabase": "${process.env.DATABASE_AUTHENTICATION_DATABASE || ''}",
        "ssl": "${process.env.DATABASE_SSL || 'true'}"
      }
    }
  }
}
