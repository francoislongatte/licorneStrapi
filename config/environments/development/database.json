{
  "defaultConnection": "default",
  "connections": {
    "default": {
      "connector": "strapi-hook-mongoose",
      "settings": {
        "client": "mongo",
        "host": "${process.env.DATABASE_HOST || 'licorne-data.documents.azure.com'}",
        "port": "${process.env.DATABASE_PORT || '10255'}",
        "database": "${process.env.DATABASE_NAME || 'strapi'}",
        "username": "${process.env.DATABASE_USERNAME || 'licorne-data'}",
        "password": "${process.env.DATABASE_PASSWORD || 'k82L6ZnVLkojhTE6tuHilRUpI4mDUsnWTr6UblZfjN7uzpAdwdlcQZRfxh89NL9tgbfaf6Uu2bAD2l99HwAgcA=='}"
      },
      "options": {
        "authenticationDatabase": "${process.env.DATABASE_AUTHENTICATION_DATABASE || ''}",
        "ssl": "${process.env.DATABASE_SSL || 'true'}"
      }
    }
  }
}
