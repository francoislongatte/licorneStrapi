{
  "defaultConnection": "default",
  "connections": {
    "default": {
      "connector": "strapi-hook-mongoose",
      "settings": {
        "client": "mongo",
        "host": "${process.env.DATABASE_HOST || 'licornecapital-dp.documents.azure.com'}",
        "port": "${process.env.DATABASE_PORT || 10255}",
        "database": "${process.env.DATABASE_NAME || 'strapi'}",
        "username": "${process.env.DATABASE_USERNAME || 'licornecapital-dp'}",
        "password": "${process.env.DATABASE_PASSWORD || 'ywM6IMoZYkLUgU9PrnRqCZ8torcOxvyX9Rl4YvFJgk2NCgntGLFOrbGQvyusX1h4RHry2bCH3n0e1N2ZBelJig=='}"
      },
      "options": {
        "authenticationDatabase": "${process.env.DATABASE_AUTHENTICATION_DATABASE || 'admin'}",
        "ssl": "${process.env.DATABASE_SSL || true}"
      }
    }
  }
}
